#include <bits/stdc++.h>
using namespace std;

int main (){
    int n,m,a,b,p,q;

    cin>>n>>m;
    cin>>a>>b;

     p = (m/b)*(n/a);
     q = (m/a)*(n/b);
    if(p>=q){
        cout<<p<<endl;
    }else{
        cout<<q<<endl;
    }
    return 0;
}
